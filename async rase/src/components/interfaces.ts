export interface Car {
  name: string | null;
  color: string | null;
  id: number;
};

export interface EngineStatus {
  velocity: number;
  distance: number;
}

export interface Winner {
  id: number | null;
  wins: number | null;
  time: number | null;
}

